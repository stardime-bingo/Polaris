# 数据、权限与同步

Polaris 是本地优先的 macOS 应用，没有自己的应用账户、后端同步服务器或内置分析服务。

## 默认保存在本机

目标、子任务、描述、标签和目标集以 JSON 保存。命令行构建通常使用 `~/Library/Application Support/Polaris/`；Xcode 沙盒构建使用应用容器内的 Application Support。外观与快捷键等设置保存在 macOS UserDefaults。

JSON 文件及导出备份没有应用层加密。请按自己的需要保护本机账户和备份文件。导入导出由用户从“更多设置”主动触发；提交 Issue 时不要附上真实备份。

## 系统权限

- **通知**：用于本机目标提醒，可在 macOS 系统设置中控制。
- **提醒事项**：可选。仅在用户启用、授权并选定列表后进行双向同步。
- **登录启动**：可选，使用 macOS 登录项能力，由设置开关控制。

全局快捷键通过系统快捷键注册实现。应用没有内置模型服务，也不会把目标发送给 AI；README 生图工具属于项目宣传资料的制作过程。

## Apple 提醒事项

开启同步后，选中目标集中的目标与绑定的 Apple 列表进行双向同步。完成、修改和删除目标会影响对应的远端提醒事项；Apple 账户与跨设备同步由 macOS / Apple 服务管理，Polaris 不保存 Apple 账户密码。

子任务是 Polaris 的本地清单：保存在本机并包含于 JSON 备份，不映射为 EventKit 原生子提醒事项，远端父目标更新不会清除它们。关闭同步或解除列表绑定后停止相应同步；这不等于删除 Apple 账户里已经存在的提醒事项。

## 演示、预览与诊断

开发预览与演示使用独立 bundle ID 和数据目录，不读取正式目标目录，不启用提醒事项读写、通知调度或登录项修改。演示内容由 `script/make_demo_data.py` 创建，不从真实数据脱敏或抽样。

普通启动不会导出验收记录。开发预览或显式 `--verification-output` 可写入本地诊断信息，包括窗口状态、导航状态和菜单栏主目标标题；这些记录只留在本机，不应直接公开。系统日志也可能包含错误或本地诊断信息。

GitHub 上的截图仅展示虚构数据；宣传封面使用生成的品牌插画。仓库不会附带用户数据、访问凭据或开发时的真实界面截图。
